<?php
// single-line comment

/*
        multi-line comment
*/

echo "DAILY PHP-WORDPRESS-LEARNING";

// Scalar types
$integer = 42;                // integer
$float = 3.14;                // float (floating point number)
$string = "Hello, world!";    // string
$boolean = true;              // boolean (true or false)

// Compound types
$array = [1, 2, 3];           // array
$object = new stdClass();     // object (instance of a class)
$callable = function () {      // callable (anonymous function)
    return "I am callable!";
};

// Special types
$nullValue = null;            // null (no value)

// Additional (less common or context-based)
$resource = fopen("file.txt", "r");  // resource (external resource, like file or DB)

?>